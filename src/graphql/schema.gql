# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthPayloadType {
  accessToken: String!
  refreshToken: String!
  user: UserType!
}

input CreateQuizInput {
  coverImage: String
  description: String!
  gameId: Int!
  questions: [QuizQuestionInput!]!
  title: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type GameType {
  coverImageUrl: String
  createdAt: DateTime!
  description: String
  id: Int!
  releasedAt: DateTime
  slug: String!
  title: String!
}

type LeaderboardEntryType {
  avatar: String
  averageScore: Float!
  rank: Int!
  totalQuizzes: Int!
  totalScore: Int!
  userId: Int!
  username: String!
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  createQuiz(input: CreateQuizInput!): QuizType!
  login(input: LoginInput!): AuthPayloadType!
  refresh(input: RefreshInput!): AuthPayloadType!
  register(input: RegisterInput!): AuthPayloadType!
  updateQuiz(id: Int!, input: UpdateQuizInput!): QuizType!
  voteQuiz(input: VoteQuizInput!): QuizType!
}

type Query {
  game(id: Int!): GameType!
  games: [GameType!]!
  leaderboard: [LeaderboardEntryType!]!
  quiz(id: Int!): QuizType!
  quizzesByGame(gameId: Int!): [QuizType!]!
  quizzesByRating: [QuizType!]!
  quizzesList: [QuizType!]!
  user(id: Int!): UserType!
  usersList: [UserType!]!
}

enum QuizDifficulty {
  easy
  hard
  medium
}

input QuizQuestionInput {
  correctAnswer: Int!
  image: String
  options: [String!]!
  text: String!
}

type QuizQuestionType {
  correctAnswer: Int!
  id: Int!
  image: String
  options: [String!]!
  order: Int!
  text: String!
}

type QuizType {
  coverImage: String
  createdAt: DateTime!
  createdBy: UserType
  createdByUserId: Int!
  description: String!
  difficulty: QuizDifficulty!
  downvotes: Int!
  game: GameType
  gameId: Int!
  id: Int!
  questions: [QuizQuestionType!]!
  title: String!
  updatedAt: DateTime!
  upvotes: Int!
}

input RefreshInput {
  refreshToken: String!
}

input RegisterInput {
  email: String!
  password: String!
  username: String!
}

input UpdateQuizInput {
  description: String
  title: String
}

enum UserRole {
  admin
  user
}

type UserType {
  avatarUrl: String
  createdAt: DateTime!
  email: String!
  id: Int!
  role: UserRole!
  username: String!
}

input VoteQuizInput {
  quizId: Int!
  vote: String
}